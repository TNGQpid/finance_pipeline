finance:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      port: 5432
      user: "{{ env_var('POSTGRES_USER') }}"
      password: "{{ env_var('POSTGRES_PASS') }}"
      dbname: "{{ env_var('POSTGRES_DB') }}"
      schema: udm
      threads: 4
